//objects have a key and a type. Type can be number, string, 
let myBook = {
    title: 'Harry Potter',
    author: 'JK Rowling',
    pageCount: 600,
}

console.log(`${myBook.title} is by ${myBook.author} and has ${myBook.pageCount} pages`)

let myData = {
    name: 'Shantel',
    city: 'Kisumu',
}

console.log(`My name is ${myData.name} and I'm from ${myData.city}.`)  //use .name(. notation) to access an item in an object. [] to access items in arrays